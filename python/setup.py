"""OverDrop — Universal Agent Communication Protocol"""
from setuptools import setup, find_packages

setup(
    name="overdrop",
    version="0.1.0",
    description="Universal Agent Communication Protocol — SQLite Mail Bus + Filesystem Protocol",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)
